# Application database used for CVs, sessions, credentials, and rate limits.
APP_DATABASE_URL=
APP_DATABASE_TOKEN=

# Existing identity database containing users, roles, and user-role mappings.
AUTH_DATABASE_URL=
AUTH_DATABASE_TOKEN=

# Optional: Space-separated extra domains allowed to embed this app in an iframe/sideapp
# Example: ALLOWED_FRAME_ANCESTORS="https://*.domain.com https://your-gat-sideapp.com"
ALLOWED_FRAME_ANCESTORS=
