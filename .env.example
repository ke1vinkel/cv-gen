# Application database used for CVs, sessions, credentials, and rate limits.
APP_DATABASE_URL=
APP_DATABASE_TOKEN=

# Existing identity database containing users, roles, and user-role mappings.
AUTH_DATABASE_URL=
AUTH_DATABASE_TOKEN=
